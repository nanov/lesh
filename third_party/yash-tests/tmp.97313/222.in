
_empty= _sp=' ' _tab='  ' _nl='
'
echoraw() {
    printf '%s\n' "$*"
}
bracket() {
    if [ $# -gt 0 ]; then printf '[%s]' "$@"; fi
    echo
}
a=a n=; unset u
(: "${u?foo bar  baz}") 2>&1 | grep -Fq 'foo bar  baz'
